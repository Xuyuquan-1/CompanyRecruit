<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hrrecruit.mapper.SysUserMapper">

    <select id="selectUserWithRoles" resultType="com.hrrecruit.entity.SysUser">
        SELECT DISTINCT u.* FROM sys_user u
        LEFT JOIN sys_user_role ur ON u.id = ur.user_id
        <where>
            u.deleted = 0
            <if test="username != null and username != ''">
                AND u.username LIKE CONCAT('%', #{username}, '%')
            </if>
        </where>
        ORDER BY u.create_time DESC
    </select>

    <select id="selectPermissionsByUserId" resultType="java.lang.String">
        SELECT DISTINCT m.permission FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId} AND m.permission IS NOT NULL AND m.permission != ''
    </select>

</mapper>